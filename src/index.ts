export { VERSION } from "./version";
export { six, ScrollTrigger } from "./api/six";
export type { Context, GlobalDefaults, ScrollTriggerVars, Timeline, TimelineVars, Tween, TweenVars } from "./api/six";

// See LEGACY_CLEANUP.md for why the old six.ts/components/plugins tree still physically exists
// but is no longer referenced here.
